\begin{tikzpicture}[
    node distance=1.5cm,
    layer/.style={draw, rectangle, minimum width=3cm, minimum height=1cm, align=center, fill=white, rounded corners},
    input/.style={draw, trapezium, trapezium left angle=70, trapezium right angle=110, minimum width=2.5cm, fill=blue!10},
    fusion/.style={draw, circle, minimum size=1.5cm, align=center, fill=orange!20},
    output/.style={draw, ellipse, minimum width=2.5cm, fill=green!20}
]

% Inputs
\node[input] (seq) {Sequence (1.2kb)};
\node[input, right=of seq] (epi) {Epigenomics (ATAC/ChIP)};

% Encoders
\node[layer, below=of seq, fill=purple!20] (mamba) {Mamba Block\\(S6 State Space)};
\node[layer, below=of epi, fill=yellow!20] (cnn) {Epigenomic Encoder\\(1D CNN)};

% Fusion
\node[fusion, below=2cm of $(mamba.south)!0.5!(cnn.south)$] (attn) {Cross-Attention\\Fusion};

% Heads
\node[layer, below=of attn] (mlp) {Prediction Head\\(MLP)};

% Output
\node[output, below=of mlp] (out) {Efficiency Score\\($\hat{y} \pm \delta$)};

% Connections
\draw[->, thick] (seq) -- (mamba);
\draw[->, thick] (epi) -- (cnn);
\draw[->, thick] (mamba) -- (attn);
\draw[->, thick] (cnn) -- (attn);
\draw[->, thick] (attn) -- (mlp);
\draw[->, thick] (mlp) -- (out);

% Labels
\node[left=0.5cm of mamba, align=right, font=\footnotesize] {Long-Range\\Dependencies};
\node[right=0.5cm of cnn, align=left, font=\footnotesize] {Chromatin\\Context};

\end{tikzpicture}
